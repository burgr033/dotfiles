{{- $email := promptString "Email" }}
{{- $name := promptString "Name" }}
{{- $isServer := promptBool "isServer (headless)" }}
{{- $isWorkMachine := promptBool "isWorkMachine" }}

[data]
        name =  "{{ $name }}"
        email = "{{ $email }}"
        isServer = "{{ $isServer }}"
        isWorkMachine = "{{ $isWorkMachine }}"
